type UserName
{
    first: String!
    last: String!
}

type User
{
    name: UserName!
}

type Query
{
    retrieveUser(id: ID!): User
}

type Mutation
{
    createUser(data: User!): User
    updateUser(id: ID!, data: User!): User
}